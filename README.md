# Notes
 - Current goal (so i dont forget like i already have):
    - Change bonklib color inline styles to classes
    - Make changeable style node
    - Check that the window index actually exists in loadUISettings and other settings related functions
    - Change windowName to modName in createMod